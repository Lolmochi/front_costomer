import 'package:flutter/material.dart';
import 'login_customer.dart';  // Import ไฟล์ login_customer

void main() {
  runApp(const MaterialApp(
    home: CustomerLoginPage(),  // กำหนดหน้าเริ่มต้นเป็น CustomerLoginPage
  ));
}
